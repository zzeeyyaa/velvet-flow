/**
 * Shared primitives for parsing "map-like" replies that can arrive in any of
 * four shapes depending on protocol version and `typeMapping`:
 *
 *   - JS `Map` (RESP3 + `typeMapping[RESP_TYPES.MAP] = Map`)
 *   - Plain object (RESP3 default)
 *   - Flat key/value `Array` (RESP2, or `typeMapping[RESP_TYPES.MAP] = Array`)
 *   - Tuple `Array` of `[key, value]` pairs (older RESP2 wire shapes)
 *
 * Helpers here normalize over all four so transforms don't have to branch
 * on protocol version.
 */
export declare function isPlainObject(value: unknown): value is Record<string, unknown>;
export declare function keyToString(key: unknown): string;
export declare function mapLikeEntries(value: unknown): Array<[string, unknown]>;
export declare function mapLikeValues(value: unknown): Array<unknown>;
export declare function mapLikeToObject(value: unknown): Record<string, unknown>;
export declare function mapLikeToFlatArray(value: unknown): Array<unknown>;
export declare function getMapValue(value: unknown, keys: Array<string>): unknown;
//# sourceMappingURL=reply-utils.d.ts.map